// === performs strict comparision between the variables including values and data types.
// == performs comparision only with the values of both variables

const number = 10;
const alpha = "10";

console.log(number == alpha);       // true
console.log(number === alpha);      // false
