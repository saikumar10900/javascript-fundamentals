// === performs strict comparision between the variables
// it checks for data type.

const number = 10;
const alpha = "10";

console.log(number == alpha);       // true
console.log(number === alpha);      // false
